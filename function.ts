// * Here function return type is number
const add = (firstNumber: number, secondNumber: number): number => {
  const sum = firstNumber + secondNumber;
  return sum;
};

// * return type void

const printMessage = (): void => {
  console.log("Hello Typescript");
};
